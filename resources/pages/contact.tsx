import { AppLayout } from '#layouts/app.layout'

interface ContactProps {}

export const ContactPage = (props: ContactProps) => {
  return (
    <AppLayout title="Contact">
      <div>contact</div>
    </AppLayout>
  )
}
